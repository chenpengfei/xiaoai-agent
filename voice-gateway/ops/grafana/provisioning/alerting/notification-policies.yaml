apiVersion: 1

policies:
  - orgId: 1
    receiver: discord-xiaoai-alerts
    group_by:
      - alertname
      - device_id
      - severity
    group_wait: 30s
    group_interval: 10m
    repeat_interval: 30m
