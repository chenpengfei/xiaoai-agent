apiVersion: 1

providers:
  - name: voice-gateway
    orgId: 1
    folder: Voice Gateway
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
